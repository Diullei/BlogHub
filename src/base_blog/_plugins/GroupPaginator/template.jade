div.pagination
	ul
		- if(page.previous) {
			li#prev
				a(href='#{(
                    config.folders.basePath 
                        ? main.config.folders.basePath 
                        : '' ) 
                    + page.previous.url
                    + (
                    config.folders.basePath 
                        ? 'index.html' 
                        : '' )}', title=page.previous.title) &larr; Previous
		- } else {
			li#prev.disabled
				a &larr; Previous
		- }
				
		li
			a(href="") Archive

		- if(page.next) {
			li#next
				a(href='#{(
                    config.folders.basePath 
                        ? config.folders.basePath 
                        : '' ) 
                    + page.next.url
                    + (
                    config.folders.basePath 
                        ? 'index.html' 
                        : '' )}', title=page.next.title) Next &rarr;
		- } else {
			li#next.disabled
				a Next &rarr;
		- }
